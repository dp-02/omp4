
### docker rum

```Bash
# 先 pull
git pull

# 停止並刪除容器
docker stop omp-app
docker rm omp-app

# 更新 image
docker build -t omp-app .

# 執行容器
docker run -d   --name omp-app   --restart always   -p 127.0.0.1:8000:8000   --env-file .env   omp-app
```
